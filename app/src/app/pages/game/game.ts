import { Component } from '@angular/core';
import { Layout } from '../../components/layout/layout';

@Component({
  imports: [Layout],
  selector: 'app-game',
  styleUrl: './game.css',
  templateUrl: './game.html',
})
export class Game {}
